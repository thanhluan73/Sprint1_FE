import styled from 'styled-components';
import WithDirection from 'settings/withDirection';
const PageWrapper = styled.div`
    padding-top:46px;
    overflow-x:hidden;
`;

export default WithDirection(PageWrapper);
